---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbittorrent-config
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  # storageClassName: longhorn-static
  resources:
    requests:
      storage: 128Mi
